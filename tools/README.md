# Tools

- `make_engine_figure.py`: runs the current OCR engine on a cleaned multimeter fixture and creates a figure showing the main image-processing stages.

Run from the project root:

    python tools/make_engine_figure.py
